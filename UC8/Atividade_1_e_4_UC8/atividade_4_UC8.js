//Atividade 4 UC8
let participantesCadastrados = ["Fulano", "Ciclano", "Beltrano"]
let dataEvento
let dataAtual
let candidatoAoCadastro 
let idadeCandidato
        // Cenário: Inscrição em um evento para um determinado dia após o dia atual.
console.log ("Entre com a data do evento:")  // candidato ao evento digita a data do evento   
if(dataEvento>dataAtual){    
         if(participantesCadastrados.length<100){
               console.log ("Informe sua idade:") 
                // aqui ---> comando para leitura
             if(idade>=18){   
               console.log ("Informe seu nome:");
               // aqui ---> comando para leitura
                participantesCadastrado.push(candidatoAoCadasto)
                console.log ("Cadastro realizado com sucesso!");
            }else{console.log ("Menor de idade não pode ser cadastrado!")}
     }else{console.log ("Não há mais vagas!")}
    }else{console.log ("Data inválida!")}